twig:
  default_path: "%kernel.project_dir%/templates"
  globals:
    cart: '@App\Service\Cart\Cart'
    paypalCheckout: '@App\Service\Checkout\PaypalCheckout'

when@test:
  twig:
    strict_variables: true
