{% extends 'base.html.twig' %}

{% block title %}
	Adresse -
	{{ user.firstName }}
	{{ user.lastName }}
	|
	{{ parent() }}
{% endblock %}

{# START : admin_address_detail #}
{% block body %}
	<main>
		<section class="page">
			<h2>Adresse :
				{{ user.firstName }}
				{{ user.lastName|upper }}</h2>
			<div
				class="list">
				{# START : card for mobile #}
				<div class="card-address card-address_color_blue" id="card-address">
					<p>
						{{ address.firstName }}
						{{ address.lastName }}
					</p>
					<p>
						{{ address.streetNumber }}
						{{ address.streetName }}
					</p>
					<p>
						{{ address.zipCode }}
						-
						{{ address.city }}
					</p>
					<p>
						{{ address.country }}
					</p>
					<p>
						{{ address.phoneNumber }}
					</p>
					<p>
						Créée le
						{{ address.createdAt|format_datetime('short', 'none', locale='fr') }}
					</p>
					<p>
						Mise à jour le
						{{ address.updatedAt|format_datetime('short', 'none', locale='fr') }}
					</p>
					<a class="page__link page__link_text-transform_uppercase page__link_background-color_safety-orange page__link_color_white" href="{{ path('admin_address_update', {'userId': user.id, 'addressId': address.id}) }}">Modifier</a>
					<a class="page__link page__link-delete page__link-delete-address page__link_text-transform_uppercase page__link_margin-bottom_1em page__link_background-color_red page__link_color_white" href="{{ path('admin_address_delete', {'userId': user.id, 'addressId': address.id, 'token':csrf_token('admin-address-delete' ~ address.id)} ) }}" data-userfirstname="{{ address.user.firstName }}" data-userlastname="{{ address.user.lastName }}">Supprimer</a>

				</div>
				{# END : card for mobile #}
				{# START : table for desktop #}
				<table class="table table_background-color_blue table_color_white table-address">
					<thead>
						<tr>
							<th>Propriétés</th>
							<th>Valeurs</th>
						</tr>
					</thead>
					<tbody>
						<tr>
							<th>Prénom</th>
							<td>{{ address.firstName }}</td>
						</tr>
						<tr>
							<th>Nom</th>
							<td>{{ address.lastName }}</td>

						</tr>
						<tr>
							<th>Numéro de rue</th>
							<td>{{ address.streetNumber }}</td>

						</tr>
						<tr>
							<th>Nom de rue</th>
							<td>{{ address.streetName }}</td>
						</tr>
						<tr>
							<th>Code postal</th>
							<td>{{ address.zipCode }}</td>
						</tr>
						<tr>
							<th>Ville</th>
							<td>{{ address.city }}</td>
						</tr>
						<tr>
							<th>Pays</th>
							<td>{{ address.country }}</td>
						</tr>
						<tr>
							<th>Numéro de téléphone mobile</th>
							<td>{{ address.phoneNumber }}</td>
						</tr>
						<tr>
							<th>Créée le</th>
							<td>{{ address.createdAt|format_datetime('short', 'none', locale='fr') }}</td>
						</tr>
						<tr>
							<th>Mise à jour le</th>
							<td>{{ address.updatedAt|format_datetime('short', 'none', locale='fr') }}</td>
						</tr>
						<tr>
							<th>Actions</th>
							<td>
								<a class=" page__link page__link_text-transform_uppercase page__link_background-color_safety-orange page__link_color_white" href="{{ path('admin_address_update', {'userId': user.id, 'addressId': address.id}) }}">Modifier</a>
								<a class="page__link page__link-delete page__link-delete-address page__link_text-transform_uppercase page__link_background-color_red page__link_color_white" href="{{ path('admin_address_delete', {'userId': user.id, 'addressId': address.id, 'token':csrf_token('admin-address-delete' ~ address.id)} ) }}" data-userfirstname="{{ address.user.firstName }}" data-userlastname="{{ address.user.lastName }}">Supprimer</a>

							</td>
						</tr>
					</tbody>
				</table>
				{# END : table for desktop #}
			</div>
		</section>
	</main>
{% endblock %}
{# START : admin_address_detail #}
